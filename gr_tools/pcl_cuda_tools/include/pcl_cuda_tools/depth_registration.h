#include <iostream>
#include <math.h>

// Kernel function to add the elements of two arrays
// Kernel function to add the elements of two arrays
// Kernel function to add the elements of two arrays
// Kernel function to add the elements of two arrays

class DepthRegistration{
  public:
    DepthRegistration();
    ~DepthRegistration();

    int getN(){
      return N;
    };

    void run();

    float *x;
    float *y;
  private:
    int N;

};
